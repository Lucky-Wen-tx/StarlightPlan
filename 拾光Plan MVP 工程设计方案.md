# 拾星Plan MVP 工程设计方案
## 一、MVP 需求定稿汇总（已确认项）
### 核心定位
个人专用本地Markdown笔记工具，无登录体系，核心对标Typora本地使用体验，待办/日程等功能留作后期迭代。

### 核心能力
- 编辑模式：类Typora所见即所得编辑，支持Markdown快捷语法触发、标准格式快捷键
- 语法支持：基础Markdown语法 + 代码块多语言高亮、表格、本地图片插入展示
- 笔记管理：平级笔记列表 + 前端实时搜索 + 永久保留的回收站，支持新建/重命名/删除笔记
- 文件能力：本地.md文件导入/导出，图片自动存入本地assets目录，相对路径引用
- 交互细节：编辑停止1秒+失焦自动保存，底部字数/字符数统计，首页欢迎页

### UI与交互
- 风格：极简现代风（参考豆包/Kimi），柔和圆角、充足留白、低饱和配色
- 布局：三栏固定结构（顶部导航栏 + 左侧笔记列表栏 + 中间主内容区）
- 主题：支持浅色/深色/跟随系统三种模式，自动记忆用户偏好
- 工具栏：顶部极简常驻栏（新建笔记、导入/导出、主题切换） + 选中文字悬浮格式栏，全局搜索置于左侧栏顶部

### 技术与部署
- 前端：React + Next.js + Tailwind CSS + TipTap 编辑器
- 后端：FastAPI（可后期无缝替换后端技术栈）
- 存储：本地文件系统持久化，所有笔记与资源均存于指定本地文件夹
- 部署：FastAPI 统一托管前端静态资源，单服务启动即可使用

---

## 二、整体系统架构
### 1. 运行模式
采用**本地单机服务模式**：FastAPI 后端同时承担API服务与前端静态资源托管职责，用户启动后端服务后，通过浏览器访问本地地址即可使用。所有笔记、图片数据全部持久化到本地指定文件夹，完全可控、无云端依赖。

### 2. 本地存储目录结构
所有数据集中存放在自定义的笔记根目录（如 `D:/ShiGuangPlan-Notes`），结构如下：
```
GrowPlan-Notes/
├── *.md                   # 正常笔记文件，文件名 = 笔记标题
├── assets/                # 统一存放笔记插入的图片资源
│   ├── xxx.png
│   └── yyy.jpg
└── .recycle/              # 回收站目录，删除的笔记移动至此（隐藏文件夹）
    └── 已删除笔记.md
```
该结构保证整个文件夹可直接备份、迁移，图片与笔记绑定不丢失，完全兼容Typora等本地Markdown编辑器。

---

## 三、后端 FastAPI 工程设计
### 1. 项目目录结构
```
StarlightPlan-server/
├── main.py                # 项目入口，注册路由、托管静态资源
├── config.py              # 全局配置：笔记根目录、服务端口等
├── schemas.py             # Pydantic 数据模型定义
├── services/
│   ├── note_service.py    # 笔记核心业务：读写、列表、删除、回收站
│   └── file_service.py    # 文件处理：图片上传、MD导入导出
├── static/                # 前端打包后的静态资源目录
└── requirements.txt       # 依赖清单
```

### 2. 核心配置项
- `NOTE_ROOT_PATH`：本地笔记根目录路径，支持用户自定义
- `SERVER_PORT`：服务端口，默认 `8000`
- `RECYCLE_DIR_NAME`：回收站目录名，默认 `.recycle`
- `ASSETS_DIR_NAME`：图片资源目录名，默认 `assets`

### 3. 核心API接口清单
| 请求方法 | 接口路径                       | 功能说明               | 核心参数                         |
| -------- | ------------------------------ | ---------------------- | -------------------------------- |
| GET      | `/api/notes`                   | 获取正常笔记列表       | 无，返回标题、创建时间、修改时间 |
| GET      | `/api/notes/{title}`           | 获取单篇笔记内容       | 笔记标题，返回Markdown纯文本     |
| POST     | `/api/notes`                   | 新建空白笔记           | 笔记标题                         |
| PUT      | `/api/notes/{title}`           | 更新（保存）笔记内容   | 笔记标题、Markdown内容           |
| DELETE   | `/api/notes/{title}`           | 删除笔记（移入回收站） | 笔记标题                         |
| GET      | `/api/recycle`                 | 获取回收站笔记列表     | 无                               |
| POST     | `/api/recycle/{title}/restore` | 恢复回收站笔记         | 笔记标题                         |
| DELETE   | `/api/recycle/{title}`         | 永久删除回收站笔记     | 笔记标题                         |
| POST     | `/api/upload/image`            | 上传图片               | 图片文件，返回相对路径           |
| POST     | `/api/import/md`               | 导入本地MD文件         | MD文件                           |
| GET      | `/api/export/{title}`          | 导出笔记为MD文件下载   | 笔记标题                         |

### 4. 关键业务逻辑说明
1. **文件操作安全**：所有文件读写均限制在笔记根目录内，过滤路径穿越风险；笔记标题自动过滤系统非法字符（`\ / : * ? " < > |`），避免保存失败。
2. **回收站逻辑**：删除笔记不直接销毁文件，仅将文件移动至 `.recycle` 目录；恢复操作将文件移回根目录，永久删除才执行真实删除。
3. **静态资源托管**：FastAPI 挂载 `static` 目录为静态资源，配置兜底路由，所有非API请求统一返回前端 `index.html`，解决单页应用刷新404问题。

---

## 四、前端 Next.js 工程设计
### 1. 技术栈补充
- UI组件：shadcn/ui（基于Tailwind的轻量组件库，高度自定义，完美适配极简风格）
- 图标：lucide-react（线性简约图标，匹配豆包/Kimi视觉风格）
- 代码高亮：Shiki（VSCode同款高亮引擎，效果优于Prism）
- 状态管理：React Hooks + Zustand（轻量状态管理，适合小型项目）

### 2. 项目目录结构（App Router 模式）
```
StarlightPlan-web/
├── app/
│   ├── layout.tsx         # 全局布局，三栏结构、主题Provider
│   ├── page.tsx           # 首页，动态渲染欢迎页/编辑页
│   └── globals.css        # 全局样式、Tailwind引入、主题变量
├── components/
│   ├── layout/
│   │   ├── Header.tsx     # 顶部导航栏
│   │   └── Sidebar.tsx    # 左侧笔记栏（含搜索、列表、回收站入口）
│   ├── editor/
│   │   ├── TiptapEditor.tsx  # TipTap编辑器核心组件
│   │   ├── FloatingToolbar.tsx # 选中文字悬浮工具栏
│   │   └── StatusBar.tsx  # 底部字数统计栏
│   └── common/
│       ├── ThemeToggle.tsx # 主题切换按钮
│       └── WelcomePage.tsx # 欢迎页组件
├── hooks/
│   ├── useTheme.ts        # 主题切换逻辑
│   └── useAutoSave.ts     # 防抖自动保存hook
├── lib/
│   ├── api.ts             # 后端请求统一封装
│   └── utils.ts           # 通用工具函数
└── package.json
```

### 3. 核心模块设计
#### 左侧边栏（Sidebar）
- 顶部：全局搜索框，输入关键词实时过滤笔记标题
- 中部：笔记列表，按最后修改时间倒序排列，选中态高亮
- 底部：回收站入口，点击切换展示回收站笔记列表
- 交互：hover显示笔记重命名、删除操作按钮

#### 顶部导航栏（Header）
- 左侧：项目名称「拾星Plan」
- 右侧：新建笔记按钮、导入按钮、导出按钮、主题切换按钮
- 风格：极简无冗余，仅保留核心操作，无多余装饰

#### 主内容区
- 默认状态：展示欢迎页，居中显示「欢迎使用拾星Plan」大标题，下方放置新建笔记主按钮
- 编辑状态：展示TipTap编辑器，底部固定状态栏，实时显示字数、字符数、最后保存时间

#### TipTap 编辑器定制
- 必选扩展：StarterKit（基础语法+快捷输入）、CodeBlockLowlight+Shiki（代码高亮）、Table（表格）、Image（图片）、Placeholder（空白提示）
- 快捷语法：支持 `# 空格` 转标题、``` 转代码块、`- 空格` 转列表等Typora同款快捷触发
- 悬浮工具栏：选中文字时弹出，包含加粗、斜体、标题、列表、链接、行内代码等格式操作
- 图片处理：支持本地文件选择，上传后端后自动插入相对路径图片，渲染时自动转换为可访问地址

### 4. 核心交互逻辑
1. **自动保存**：通过防抖hook实现，编辑内容停止变化1秒后自动触发保存；编辑器失焦时立即保存，避免频繁读写磁盘。
2. **主题系统**：基于Tailwind CSS dark模式实现，支持浅色/深色/跟随系统三种模式，偏好存储在localStorage。
3. **笔记搜索**：前端本地实时过滤，无需请求后端，输入即出结果，适配个人自用小数据量场景。

---

## 五、分阶段开发落地路线（可按顺序直接执行）
### 阶段1：基础架子与核心编辑器MVP
**目标**：跑通前后端联动，实现单篇笔记的编辑与本地持久化
1. 初始化FastAPI项目，配置目录结构，实现静态资源基础托管
2. 初始化Next.js项目，配置Tailwind CSS，搭建三栏基础布局
3. 集成TipTap编辑器，完成基础语法、代码高亮、表格功能调试
4. 开发笔记读写核心接口，打通前后端文件读写链路
5. 实现基础防抖自动保存功能
**完成标准**：打开网站可编辑内容，刷新后内容保留在本地MD文件中

### 阶段2：多笔记管理体系完善
**目标**：实现完整的笔记生命周期管理
1. 开发笔记列表、新建、重命名、删除接口，完成左侧栏列表交互
2. 落地回收站全流程逻辑（移入、恢复、永久删除）
3. 实现前端实时笔记搜索功能
4. 开发MD文件导入、导出接口与前端交互
5. 完成欢迎页开发，默认展示欢迎态
**完成标准**：支持多篇笔记管理，删除可恢复，可导入导出本地文件

### 阶段3：体验优化与UI打磨
**目标**：对齐极简视觉风格，完善核心交互体验
1. 实现选中文字悬浮工具栏，精简顶部工具栏样式
2. 完成三主题切换，全页面适配深浅色模式
3. 开发底部状态栏，实现字数、字符数实时统计
4. 打通本地图片上传、插入、展示全流程
5. 补充格式快捷键、Markdown快捷语法适配
6. 全局UI细节打磨：圆角、阴影、留白、过渡动效
**完成标准**：视觉风格统一，交互流畅，核心体验对标Typora基础版

### 阶段4：部署与使用优化
**目标**：降低日常使用门槛，达到稳定可用状态
1. 前端打包为静态文件，集成到FastAPI的static目录
2. 编写Windows一键启动脚本（bat文件），双击启动服务并自动打开浏览器
3. 补充配置说明，支持自定义笔记根目录
4. 全功能测试与bug修复
**完成标准**：双击启动脚本即可正常使用，所有MVP功能稳定

---

## 六、核心实现避坑指引
1. **TipTap导出标准Markdown**：TipTap默认输出HTML，需安装 `tiptap-markdown` 扩展实现标准MD导出，保证文件兼容其他编辑器。
2. **图片路径兼容**：保存到MD文件时保留纯相对路径（如 `assets/xxx.png`），编辑器渲染时再拼接为本地可访问URL，保证文件迁移后正常显示。
3. **单页路由兜底**：FastAPI需配置通配路由，所有 `/api` 之外的请求都返回 `index.html`，避免刷新页面出现404。
4. **文件名合法性校验**：新建/重命名笔记时，自动过滤Windows系统非法文件名，避免文件读写报错。

